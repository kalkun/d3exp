import Ember from 'ember';

export default Ember.Route.extend({
    model : function () {
        // return Ember.$.get("/Aalborggenericc.csv");
        // return Ember.$.get("/im0001.csv");
    }
});
